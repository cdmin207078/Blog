@using Blog.Models

<div class="row">
    <div class="col-md-12">
        @Html.ActionLink("写文章", "Add", null, new { @class = "btn btn-default" })
    </div>
</div>
<br />
<div class="row">
    <div class="col-md-12">
        <table class="table table-condensed table-hover">
            <thead>
                <tr>
                    <th>标题</th>
                    <th style="width:10%">发表时间</th>
                    <th style="width:10%">操作</th>
                </tr>
            </thead>
            <tbody>
                @{
                    var articles = ViewBag.Articles as List<Article>;
                    if (articles == null || articles.Count == 0)
                    {
                        <tr>
                            <td colspan="3"><p class="bg-warning">暂无数据</p></td>
                        </tr>
                    }
                    else
                    {
                        foreach (Article item in ViewBag.Articles)
                        {
                            <tr>
                                <td>@item.Title</td>
                                <td><em title="@item.CreateTime">@item.CreateTime.ToString("yyyy-MM-dd")</em></td>
                                <td>
                                    @Html.ActionLink("编辑", "Edit", new { id = item.Id }, new { @class = "btn btn-primary btn-xs" })
                                </td>
                            </tr>
                        }
                    }

                }


            </tbody>
        </table>
    </div>
</div>