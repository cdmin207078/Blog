# 认证过滤器在 ASP.NET Web API 2 的使用

> Authentication Filters (认证过滤器) 是认证HTTP请求的组件。 Web API 2和MVC 5都支持 Authentication Filters (认证过滤器)，但它们略有不同，主要是过滤器接口的命名约定。 这篇文字讲解一下Web API中的 Authentication Filters (认证过滤器)。

